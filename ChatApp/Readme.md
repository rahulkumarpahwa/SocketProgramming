# Chat App Using the Socket Programming:
- This app shows the chat app between the client and server using the socket.
- Both the sides will have the Scanner which will take the input unless type EXIT in some way or the other.
- While the print function will be written once and not under the loop.